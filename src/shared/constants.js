export const STORAGE_KEYS = {
  enabled: "enabled",
  autoSkip: "autoSkip",
  status: "status"
};

export const CLICK_COOLDOWN_MS = 1500;

export const RUNTIME_ACTIONS = {
  trustedClick: "trustedClick"
};
